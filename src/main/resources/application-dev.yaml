server:
  port: 8081
spring:
  jpa:
    database: POSTGRESQL
    generate-ddl: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/encounter
    username: user
    password: secret
5e-srd-api:
  url: https://www.dnd5eapi.co/api/